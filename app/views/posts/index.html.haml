%table.table
  %thead
    %th
      \#
    %th
      标题
    %th
      日期
  %tbody
    -@posts.each do |post|
      %tr
        %td
          =post.id
        %td
          =link_to post.title, post_path(post.id)
        %td
          =l post.created_at
